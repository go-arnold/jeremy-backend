# Core
Django==6.0.6
djangorestframework==3.17.1

# Authentication
djangorestframework-simplejwt==5.5.1
dj-rest-auth==7.2.0
django-allauth==65.18.0

# Database
psycopg2-binary==2.9.12
django-db-connection-pool[psycopg2]==1.2.6

# Cache & Async
redis==8.0.0
django-redis==7.0.0
celery==5.6.3
django-celery-beat==2.9.0

# Storage
cloudinary==1.44.2
django-cloudinary-storage==0.3.0
django-storages==1.14.6

# API
drf-spectacular==0.29.0
django-cors-headers==4.9.0
django-filter==25.2

# Search
django-elasticsearch-dsl==9.0
# Pinned below django-elasticsearch-dsl's own resolved latest (9.4.x): that
# combination silently produces empty _source on every indexed document (a
# regression in elasticsearch.dsl's AttrDict internals between 9.0.3 and
# 9.4.1) — confirmed against a live ES 9.0.2 cluster. Do not bump without
# re-verifying prepare() actually populates fields.
elasticsearch==9.0.3

# Utils
python-decouple==3.8
Pillow==12.2.0
django-extensions==4.1
cryptography==49.0.0

# Seed data
Faker==40.23.0

# Server
gunicorn==26.0.0
uvicorn[standard]==0.49.0

# Dev
pytest-django==4.12.0
factory-boy==3.3.3
